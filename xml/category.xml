<alps version="1.0">
  <doc>
     Author: Mike Amundsen (mca@mamund.com)

     Simple service to manage category tags

     Servers SHOULD support the "category" semantic element.
     Servers MAY support one or more of the transition elements.
  </doc>

  <!-- data elements -->
  <descriptor id="category" type="semantic" />

  <!-- tranistion elements -->
  <descriptor id="list" type="safe" rt="#category"/>
  <descriptor id="search" type="safe" rt="#category"/>
  <descriptor id="create" type="unsafe" rt="#category"/>
  <descriptor id="update" type="idempotent" rt="#category" />
  <descriptor id="remove" type="idempotent" rt="#category"/>

</alps>
